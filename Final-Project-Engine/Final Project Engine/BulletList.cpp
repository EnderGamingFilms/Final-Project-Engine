#include "Init.h"

//void BulletList::iterate(EnemyList &enemies, DestructorList &destructors)
//{
//	float enemyX = 0, enemyY = 0;
//	for (Node * current = this->getStart(); current != nullptr; current = current->getNext())
//	{
//		Bullet *currentBullet = dynamic_cast<Bullet *>(current->getData());
//		Enemy * currentEnemy = dynamic_cast<Enemy *>(enemies.getStart());
//		for (int enemyIndex = 0; currentEnemy != nullptr; enemyIndex++) //Check current bullet against all enemies in list
//		{
//
//		}
//	}
//}